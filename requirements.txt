# --- Manipulation JSON et XML ---
lxml==5.1.0            # Génération et parsing XML
xmlschema==3.0.2       # Validation stricte contre XSD

# --- Cryptographie ---
cryptography==42.0.5   # Hash SHA-256, signature numérique

# --- Base de données ---
SQLAlchemy==2.0.25     # ORM pour PostgreSQL/MySQL/SQLite
psycopg2-binary==2.9.9 # Driver PostgreSQL (si tu utilises PostgreSQL)
mysqlclient==2.2.4     # Driver MySQL/MariaDB (si tu utilises MySQL)
               # Déjà inclus dans Python standard, pas besoin d’installer

# --- Outils et tests ---
pytest==8.3.3          # Tests unitaires
python-dotenv==1.0.1   # Gestion des variables d’environnement (ex: credentials DB)